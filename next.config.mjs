/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: '/lab10kacper',
    assetPrefix: '/lab10kacper',
    trailingSlash: true,
    reactStrictMode: true,
};

export default nextConfig;
